The docker relevant files located in this specific directory are ONLY related
to the NB-IoT webapplication. This means that if one wishes to simply only run this single site,
Execute the ./build_and_deploy.sh script which builds the Docker image, and executes the container. 
The relevant configurations, ports, etc. are located in the docker-compose.yml.

